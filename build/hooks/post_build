#!/bin/bash

docker build --platform linux/arm64 -f $DOCKERFILE_PATH -t $IMAGE_NAME-linux-arm64 .
docker push $IMAGE_NAME-linux-arm64

docker manifest create $IMAGE_NAME-all $IMAGE_NAME $IMAGE_NAME-linux-arm64
docker manifest push $IMAGE_NAME-all
